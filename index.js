const { sum, average, unique, reverse, shuffle, filterFalsy, flatten, sortByLength, chunk, rotate } = require('./array-utils');

module.exports = {
  sum,
  average,
  unique,
  reverse,
  shuffle,
  filterFalsy,
  flatten,
  sortByLength,
  chunk,
  rotate
};
