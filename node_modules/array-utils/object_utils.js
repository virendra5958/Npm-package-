
function init(context) {
    context = context || this;

    require('./namespace').namespace('Object.prototype', context);

    context.Object.prototype.add = function(obj) {
        for (k in obj) {
            this[k] = obj[k];
        }
        return this;
    };
}

init();

exports.init = init;