const arrayUtils = require('./index');

const arr = [1, 2, 3, 4, 5];
console.log(arrayUtils.sum(arr)); // Output: 15
console.log(arrayUtils.average(arr));
console.log(arrayUtils.unique([1, 2, 2, 3, 3])); 
